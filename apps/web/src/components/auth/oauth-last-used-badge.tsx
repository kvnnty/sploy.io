export function OAuthLastUsedBadge() {
  return (
    <span className="absolute -top-2 right-2 shrink-0 rounded-md bg-muted px-2 py-0.5 text-[10px] font-medium uppercase tracking-wide text-muted-foreground border border-border">
      Last used
    </span>
  );
}
